import { SyncStackFile } from './types';
/**
 * Load the stack file
 * @param {string} stackPath the stack file path
 * @returns {SyncStackFile} sync stack file
 */
export declare const loadStack: (stackPath: string) => SyncStackFile;
